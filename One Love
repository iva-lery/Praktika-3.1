clc
clear
% generate a vector [-500, 400]
a = -500:0.5:400;
sum = 0;
% iterate through vector "a"
for i=1:1:length(a)
% check that item is integer
if not(mod(a(i),1) == 0)
sum = sum+a(i);
% or positive number
elseif a(i) > 0
sum = sum+a(i);
end
end
% print sum result
sum
